// js/config.js
export const CONFIG = {
    // Game dimensions
    GAME_WIDTH: 800,
    GAME_HEIGHT: 600,

    // Bird settings
    BIRD_SIZE: 30,
    GRAVITY: 0.5,
    JUMP_FORCE: -10,

    // Pipe settings
    PIPE_WIDTH: 60,
    PIPE_GAP: 200,
    PIPE_SPEED: 3,
    PIPE_SPAWN_DISTANCE: 300,

    // Game settings
    FPS: 60,
    DEBUG_MODE: false,

    // Difficulty settings
    DIFFICULTY_INCREASE_RATE: 0.1,
    MAX_PIPE_SPEED: 6,
    MIN_PIPE_GAP: 150,
};
